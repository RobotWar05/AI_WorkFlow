# Tool contract

- Tool ID and single purpose:
- Input/output schema:
- Side-effect class:
- Timeout:
- Idempotency and deduplication key:
- Dry-run behavior:
- Approval rule:
- Expected error codes:

External write, deploy, credentials, hardware and destructive tools require an action-time approval gate. Machine form: `../schemas/v1/tool-contract.schema.json`.
