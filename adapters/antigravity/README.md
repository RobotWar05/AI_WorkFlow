# Antigravity adapter

Generated agents live in `.agents/agents/`. Only documented tool names are emitted. Workspace mode is selected at invocation; `branch` requires a Git baseline. Teamwork preview is not part of the portable baseline.
