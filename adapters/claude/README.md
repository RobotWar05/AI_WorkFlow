# Claude adapter

Generated agents live in `.claude/agents/`. Read-only roles use a tool allowlist. Claude Agent Teams stays experimental and is not required by portable workflows. This adapter is static-only until a local Claude runtime test is available.
