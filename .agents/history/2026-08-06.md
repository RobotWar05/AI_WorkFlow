# 2026-08-06 — Portable project memory profiles

## Verified change

- Added `none`, `balanced` and `deep` project-memory profile definitions and a single-owner bootstrap route.
- Added project-local Obsidian template packages. `balanced` provides approval-gated sourced Markdown; `deep` adds claim/health and manual-index templates. `none` creates no vault.
- Added `tools/bootstrap_memory_profile.py`: dry-run by default, works before a target control plane exists, refuses overwrite, and requires both an existing `.agents/` and `--apply` to create a projection.
- Registered `obsidian-second-brain` and `agentmemory` as reference-only candidates. No external code, installer, MCP server, hook, scheduler or Agentmemory integration was installed.
- Committed the verified framework change locally on `main`; no remote push was performed.

## Evidence

- `py -B tools/validate_all.py` passed: skill/profile validation, context validation, 96 skill eval cases, V2 validation, 18 Python tests, sensitive-data scan and Markdown-link check.
- `py -B tools/workflowctl.py check-adapters` passed.
- `tests/test_bootstrap_memory_profile.py` covers dry run, None, Balanced, Deep and overwrite refusal.

## Limits

- Agentmemory remains deferred and unpinned; it requires an independent exact-revision, license, source-code and local-runtime review before any use.
- Local-only is a declared control-plane policy. Runtime transport, model provider behavior and network isolation require runtime-specific verification.
- No remote push, deployment or target-project projection was performed.
