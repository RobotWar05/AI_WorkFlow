# Start a new chat

```text
Read AGENTS.md, .agents/context/current.md and the relevant workflow. If `.agents/memory-profile.json` exists, read it before selecting a knowledge skill; do not preload the whole `knowledge/` vault. Read handoff only for resume/transfer; retrieve history only when evidence is needed. Summarize objective, verified facts, unknowns and next action, then wait for a concrete task or approval.
```
